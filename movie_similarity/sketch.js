var data

function preload(){
    data = loadJSON('')
    // console.log(loadJSON('movie.json'))
}

function setup(){
    noCanvas()
    // var users = data.users
    // var person1 = createSelect('')
    // var person2 = createSelect('')

    console.log(data)
}